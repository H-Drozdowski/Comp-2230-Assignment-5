# Debugging analysis

## Trees json being set 1 & 2
These two screenshots show the searchPark function returning the json being fetched. Its then set to a variable called trees and used to create dynamic elements for the website.

## Tree info being displayed 1 & 2
Shows the website before a tree info box is created and shows the current tree being iterated through in the list thats being set with the createTreeInfoBox function.

## Random Tree image 1 & 2
Shows the random tree image being selected. Shows the src changing between lines.  